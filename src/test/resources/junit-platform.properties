junit.jupiter.execution.parallel.enabled=true
junit.jupiter.extensions.autodetection.enabled=true

# all the test classes and test methods executed in parallel
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.config.strategy=dynamic

# OR classes in parallel but methods in same thread
#junit.jupiter.execution.parallel.mode.default=same_thread
#junit.jupiter.execution.parallel.mode.classes.default=concurrent

# OR classes sequentially but their methods in parallel
#junit.jupiter.execution.parallel.mode.default=concurrent
#junit.jupiter.execution.parallel.mode.classes.default=same_thread

# to set the default test instance lifecycle mode to Lifecycle.PER_CLASS
#junit.jupiter.testinstance.lifecycle.default = per_class